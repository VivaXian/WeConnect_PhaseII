# 设备解绑 / 设备管理 功能方案

> 状态：需求与方案讨论稿（待评审 → 冻结后进入实现）
> 记录时间：2026-08
> 目的：沉淀「认证用户 / 授权用户」设备解绑与设备管理能力的完整需求与分期方案，避免后续沟通丢失上下文。

---

## 1. 背景与目标

WeConnect Phase II 需要为用户提供**对自己设备列表的管理能力**，第一步是「解绑」。

- **认证用户**：设备全部来自本人扫码 / 报修时的自主绑定。解绑 = 从列表移除。
- **授权用户**：设备数量大，且来自**后台系统按授权院区同步推送**；此外授权用户也可能有自己绑定的设备。授权用户往往由认证用户升级而来，解绑体验不能与认证用户差异过大。

目标：提供多个「管理设备」入口，支持**单台解绑**与**批量管理**，并在数据层面正确区分设备来源，保证不同角色、不同用户之间互不影响。

---

## 2. 现状与地基（实现前必须知道）

- **当前「绑定」是纯 UI 演示**：`scan-device-input-page.tsx` 点「绑定设备」只切换本地 `bindConfirmed`，未写入任何 store。
- **两个列表页直接渲染整个静态 `deviceList`**（仅按院区过滤），没有「本用户已绑定设备」的真实数据。
- `Device.isDistributedDevice` 字段仅用于合同/超声逻辑，**不代表设备来源**，不能复用。

**结论**：解绑要生效，必须先引入真实的「绑定 / 解绑状态」数据源，并把扫码绑定改成真实写入。这是所有后续功能的地基（Phase 1 第 0 步）。

---

## 3. 核心概念模型

### 3.1 设备来源（source）

给设备增加来源维度：

| source | 含义 | 出现于 |
|---|---|---|
| `self` | 用户自己扫码 / 报修绑定 | 认证用户（全部）、授权用户（部分） |
| `system` | 后台按授权院区同步推送 | 仅授权用户 |

> 认证用户**永远不会**收到 `system` 推送设备，只有自绑定。

### 3.2 用户视角 vs 后台视角（关键澄清）

- **用户视角：统一叫「解绑」。** 用户不需要区分"隐藏"还是"删除"，那是后台的实现细节。所有从列表移除设备的动作，对用户一律呈现为「解绑」。
- **后台视角：按来源区分处理。**
  - `self` 设备解绑 → 断开绑定关系，从该用户列表移除。
  - `system` 设备解绑 → 记录一条「该用户已解绑此设备」，从该用户列表移除，**且未来同步不再把该设备推给这个用户**（除非用户主动同步/恢复）。

### 3.3 每用户隔离（重要）

- 授权用户的推送设备列表**从系统角度是稳定的**（设备属于医院）。
- 但**一家医院可有多个授权用户**，每个人的管理需求不同，**解绑集合各不相同、互不影响**。
- 因此解绑 / 排除记录必须**以「授权用户身份」为作用域**，而不是以医院或设备为作用域。
  - Demo 阶段为单用户，用本地 persist store 即可；真实后台需按用户 ID 存储。

### 3.4 再推送抑制（re-push suppression）

- 未来授权用户名下**新增设备要推送**时，后台必须**排除该用户此前已解绑的推送设备**，不能把用户主动移除的设备又推回来。
- 数据上通过每用户的「已解绑推送设备集合（excluded set）」实现过滤。

---

## 4. 授权用户界面：两个清晰的列表

授权用户在「设备管理」中，将设备**按来源分成两组清晰展示**（文案待定，示意）：

1. **「系统同步设备」**（source = system）
   - 提示语：例如「这些设备来自授权院区同步，解绑后可随时重新同步回来」。
   - 让用户明确知道：推送设备的解绑是"可逆"的（后台仍保有，可再同步）。
2. **「我绑定的设备」**（source = self）
   - 自己扫码 / 报修绑定，解绑即断开。

> 认证用户只有一组（自绑定），无需分组。这样从认证用户升级为授权用户时，界面是"在原有单列表上多出一组系统同步设备"，体验连续。

---

## 5. 详情页解绑 → 暂存到「设备管理」（方案建议）

**诉求**：通过设备详情页解绑的设备，是否可以「只是从主页消失，但暂存到『我的 → 设备管理』」，让用户在真正的管理页完成 解绑 = 删除？

**建议方案（推荐）——「回收区 / 已解绑」缓冲，统一入口，单一措辞：**

- 详情页「解绑」点击后：设备**立即从主列表（我的设备）消失**（符合用户"解绑=消失"的直觉），并**进入「设备管理」页的『已解绑』区**。
- 「已解绑」区支持：
  - **恢复**（撤销解绑，放回主列表）——提供后悔药，降低误操作成本。
  - 对 `self` 设备可进一步「彻底删除 / 清空」（永久断开）。
  - 对 `system` 设备，"已解绑"即等于"已排除再推送"；恢复 = 重新同步回来。
- 操作后主列表顶部给 **撤销 toast**（"已解绑，撤销"）。

**为什么这样比"详情页解绑后设备仍留在主列表待处理"更好：**
- 保持"解绑 = 从主页消失"的心智一致，不产生"点了解绑却还在列表"的困惑。
- 仍满足你要的"暂存 + 在管理页做最终处理"：缓冲区就在设备管理页，且天然带撤销。
- 单台解绑与批量解绑**汇入同一个『已解绑』区**，逻辑统一。

> 备选（若你更想要"两段式"）：详情页动作改叫「移出主页」，设备进入设备管理「待整理」，在那里再「解绑」。缺点是与"统一叫解绑"的原则冲突，且多一步。默认采用上面的推荐方案。

---

## 6. 入口与交互

### 入口 A｜设备详情 · 总览页 —— 单台解绑
- 底部「解绑设备」（destructive 风格）→ Filament `Dialog` 二次确认 → 返回并 toast「已解绑，撤销」。
- 设备进入「设备管理 · 已解绑」区。

### 入口 B｜「我的」页 —— 批量管理（两个入口都做）
1. 菜单区新增一行「设备管理」。
2. Hero 上的「关联 N 台设备」chip 也点进同一页。

### 设备管理页
- 顶部：设备总数 / 已选数 / 全选。
- 主体：
  - 认证用户：单列表（自绑定），多选批量解绑。
  - 授权用户：**两组**（系统同步 / 我绑定），各自多选、各自批量解绑；系统同步组带"可同步回来"提示。
- 「已解绑」区：展示已解绑设备，支持恢复；`self` 可彻底删除。
- 批量二次确认弹窗：显示将解绑 N 台。

---

## 7. 数据 & Store 设计（Demo 层）

新增持久化 store（参照 `device-custom-names-store` + `persist`）：

```
device-binding-store（persist，按用户作用域）
  selfBoundIds: string[]     // 用户自绑定且在册的设备
  removedIds: string[]       // 用户已解绑的设备（回收区）；
                             //   对 system 设备同时充当"再推送排除集"
  bindSelf(id)               // 扫码/报修绑定时写入
  unbind(id)                 // 解绑：移入 removedIds
  restore(id)                // 从已解绑恢复
  purge(id)                  // self 设备彻底删除
```

**主列表派生规则（两个列表页统一）：**
- 认证用户：`self 且在 selfBoundIds 且不在 removedIds`
- 授权用户：`(system 且在授权院区 且不在 removedIds)` ∪ `(self 且在 selfBoundIds 且不在 removedIds)`

**再推送抑制**：后台/模拟同步新增 system 设备时，`id ∈ removedIds` 的一律不加入。

---

## 8. 升级路径与「智能对账」（认证 → 授权）

认证用户只有 `self` 设备。升级为授权用户时：

1. **保留自绑定**：原 `self` 设备保持 `self` 且保持在册。
2. **开始系统推送**：按新授权院区同步 `system` 设备。
3. **智能去重 / 对账**（"智能一点"）：
   - 若某台自绑定设备**同时落在新授权推送范围内** → **视为同一台，去重**，不重复出现两次。
   - 推荐：合并后以 `system` 呈现（它已是权威推送设备、可同步回来），但因用户原本就持有，保持"在册/active"，不因合并被移除。
4. **尊重历史解绑**：升级前用户已解绑的设备，若现在进入推送范围 → 因其在 `removedIds`，**默认不自动推回**（与"再推送抑制"一致，尊重用户意愿）；用户可在「已解绑」里主动恢复。

---

## 9. 分期计划

### Phase 1 —— 认证用户解绑（本期）
- 地基：`device-binding-store`（真实 selfBoundIds / removedIds）；扫码「绑定」改为真实写入。
- 单台解绑：设备详情总览页 + 二次确认 + 撤销 toast。
- 批量管理：「我的」菜单入口 + hero chip 入口 → 设备管理页（单列表多选解绑）。
- 「已解绑」区：恢复 + 彻底删除。
- 组件从一开始就按「来源感知」设计（预留 system 分支）。

### Phase 2 —— 授权用户解绑（未来）
- `source = system` 推送设备接入。
- 设备管理页双分组（系统同步 / 我绑定）+ "可同步回来"提示。
- 再推送抑制（removedIds 过滤后台同步）。
- 升级智能对账（去重、尊重历史解绑）。
- 每用户隔离（真实后台按用户 ID 存储解绑集）。

> 设计原则：Phase 1 即按"来源感知 + 回收缓冲 + 统一解绑措辞"落地，Phase 2 只补 `system` 分支与后台对账，不推倒重来。

---

## 10. 已确认的决策

- ✅ 用户侧一律叫「解绑」，不暴露隐藏/删除区分（后台自行区分 self / system 处理）。
- ✅ 推送设备解绑对用户 = 消失；后台记录并抑制再推送。
- ✅ 解绑状态按授权用户隔离，多授权用户互不影响。
- ✅ 授权用户界面按来源分两组，系统同步组提示可随时同步回来。
- ✅ 详情页解绑 → 暂存到设备管理「已解绑」区，支持恢复；采用"立即从主页消失 + 回收缓冲"的推荐方案。
- ✅ 批量管理入口：菜单行 + hero「关联 N 台设备」chip 双入口。
- ✅ 认证用户只有自绑定；升级为授权用户时做智能去重与历史解绑尊重。

## 11. 待细化 / 待定

- 授权用户两组列表的**具体文案**（分组标题、系统同步组的可同步提示语）。
- 「已解绑」区是否需要**保留时长 / 自动清理**策略。
- 详情页解绑撤销 toast 的**停留时长**与交互细节。
- 演示数据：Phase 2 时给 `deviceList` 标注若干 `source: 'system'` 以演示两种解绑差异。
