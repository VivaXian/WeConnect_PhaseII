import { style } from '@vanilla-extract/css';
import { tabsTokens } from '@filament/react/themes/components/tabs';

const tabIndicator = {
  content: '""',
  flexShrink: 0,
  width: tabsTokens.indicator.width,
  height: 24,
  borderRadius: `0 ${tabsTokens.indicator.border.radius} ${tabsTokens.indicator.border.radius} 0`,
  background: tabsTokens.indicator.color,
} as const;

export const rdStyles = {
  page: style({
    display: 'flex',
    flexDirection: 'column',
    height: '100%',
    overflowY: 'auto',
    background: '#F0F9FF',
  }),
  subHeader: style({
    background: '#ffffff',
    padding: '12px 16px 14px',
    borderBottom: '1px solid #f0f0f0',
    flexShrink: 0,
  }),
  subHeaderMeta: style({
    fontSize: 12,
    color: '#6b7280',
    marginBottom: 4,
  }),
  subHeaderTitle: style({
    fontSize: 20,
    fontWeight: 700,
    color: '#0a0a0a',
    marginBottom: 4,
  }),
  subHeaderTagline: style({
    fontSize: 13,
    color: '#374151',
  }),
  sections: style({
    display: 'flex',
    flexDirection: 'column',
    gap: 8,
    padding: '8px 0 24px',
  }),
  section: style({
    background: '#fff',
    padding: '8px 16px 16px',
  }),
  sectionTitle: style({
    display: 'flex',
    alignItems: 'center',
    gap: 8,
    minHeight: 40,
    marginBottom: 4,
    fontSize: 16,
    lineHeight: '24px',
    fontWeight: 700,
    color: tabsTokens.text.color.selected,
    selectors: {
      '&::before': tabIndicator,
    },
  }),
  engineerRow: style({
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'space-between',
  }),
  engineerInfo: style({
    display: 'flex',
    alignItems: 'center',
    gap: 8,
  }),
  engineerActions: style({
    display: 'flex',
    alignItems: 'center',
    gap: 10,
  }),
  sectionHeader: style({
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'flex-start',
    justifyContent: 'space-between',
    gap: 8,
  }),
  sectionHeaderAction: style({
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    gap: 2,
    flexShrink: 0,
    padding: 0,
    border: 'none',
    background: 'none',
    fontSize: 13,
    lineHeight: '20px',
    color: '#0072db',
    cursor: 'pointer',
  }),
  sectionHeaderActionIcon: style({
    width: 14,
    height: 14,
    flexShrink: 0,
  }),
  sectionEntry: style({
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    gap: 8,
    width: '100%',
    padding: 0,
    border: 'none',
    background: 'none',
    textAlign: 'left',
    cursor: 'pointer',
  }),
  sectionEntryTitle: style({
    display: 'flex',
    alignItems: 'center',
    gap: 8,
    minHeight: 40,
    fontSize: 16,
    lineHeight: '24px',
    fontWeight: 700,
    color: tabsTokens.text.color.selected,
    selectors: {
      '&::before': tabIndicator,
    },
  }),
  sectionEntryIcon: style({
    width: 16,
    height: 16,
    flexShrink: 0,
    color: '#9aa1ac',
  }),
  historyLink: style({
    alignSelf: 'flex-start',
    background: 'none',
    border: 'none',
    padding: '4px 4px 0',
    fontSize: 13,
    color: '#6a7282',
    cursor: 'pointer',
  }),
  engineerName: style({
    fontSize: 14,
    fontWeight: 600,
    color: '#0a0a0a',
  }),
  engineerRole: style({
    fontSize: 12,
    color: '#6a7282',
  }),
  callBtn: style({
    width: 36,
    height: 36,
    borderRadius: '50%',
    border: '1px solid #0072db',
    background: '#fff',
    color: '#0072db',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    cursor: 'pointer',
    flexShrink: 0,
  }),
  timeline: style({
    display: 'flex',
    flexDirection: 'column',
    paddingLeft: 8,
  }),
  timelineNode: style({
    display: 'flex',
    gap: 12,
    alignItems: 'flex-start',
  }),
  timelineLeft: style({
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    flexShrink: 0,
  }),
  timelineDot: style({
    width: 24,
    height: 24,
    borderRadius: '50%',
    background: '#0072db',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    flexShrink: 0,
    color: '#ffffff',
  }),
  timelineDotInactive: style({
    width: 24,
    height: 24,
    borderRadius: '50%',
    background: '#d1d5db',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    flexShrink: 0,
    color: '#ffffff',
  }),
  timelineLine: style({
    width: 2,
    flex: 1,
    background: '#0072db',
    minHeight: 16,
    margin: '2px 0',
  }),
  timelineLineInactive: style({
    width: 2,
    flex: 1,
    background: '#d1d5db',
    minHeight: 16,
    margin: '2px 0',
  }),
  timelineContent: style({
    paddingBottom: 16,
    flex: 1,
  }),
  timelineLabel: style({
    fontSize: 14,
    fontWeight: 500,
    color: '#0a0a0a',
    marginBottom: 2,
  }),
  timelineDate: style({
    fontSize: 12,
    color: '#6a7282',
    marginBottom: 2,
  }),
  timelineDetail: style({
    fontSize: 12,
    color: '#6a7282',
  }),
  descRow: style({
    display: 'flex',
    gap: 8,
    marginBottom: 8,
    alignItems: 'flex-start',
  }),
  descLabel: style({
    fontSize: 13,
    color: '#6a7282',
    minWidth: 72,
    flexShrink: 0,
  }),
  descValue: style({
    fontSize: 13,
    color: '#0a0a0a',
    flex: 1,
  }),
  photoGrid: style({
    display: 'grid',
    gridTemplateColumns: '1fr 1fr 1fr',
    gap: 6,
    marginTop: 8,
  }),
  photoPlaceholder: style({
    aspectRatio: '1',
    background: '#ececf0',
    borderRadius: 8,
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    fontSize: 20,
    color: '#9ca3af',
  }),
  quietInquiry: style({
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    gap: 4,
    width: '100%',
    padding: '6px 16px',
    border: 'none',
    background: 'none',
    fontFamily: 'inherit',
    fontSize: 12,
    lineHeight: '18px',
    color: '#6a7282',
    cursor: 'pointer',
  }),
  quietInquiryAction: style({
    color: '#0072db',
  }),
  noticeBar: style({
    marginTop: 10,
    paddingTop: 10,
    borderTop: '1px solid #f0f0f0',
    fontSize: 12,
    lineHeight: '18px',
    color: '#6a7282',
  }),
  noticeLink: style({
    border: 'none',
    background: 'none',
    padding: 0,
    fontFamily: 'inherit',
    fontSize: 12,
    lineHeight: 'inherit',
    color: '#0072db',
    cursor: 'pointer',
  }),
  timelineDotSmallDot: style({
    width: 8,
    height: 8,
    borderRadius: '50%',
    background: 'currentColor',
  }),
  progressStepper: style({
    display: 'flex',
    alignItems: 'flex-start',
    padding: '8px 0 16px',
  }),
  progressStepNode: style({
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    gap: 6,
    flexShrink: 0,
  }),
  progressStepDot: style({
    width: 32,
    height: 32,
    borderRadius: '50%',
    background: '#0072db',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    color: '#ffffff',
  }),
  progressStepDotInactive: style({
    width: 32,
    height: 32,
    borderRadius: '50%',
    background: '#d1d5db',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    color: '#ffffff',
  }),
  progressConnector: style({
    flex: 1,
    height: 3,
    background: '#0072db',
    marginTop: 14,
  }),
  progressConnectorInactive: style({
    flex: 1,
    height: 3,
    background: '#d1d5db',
    marginTop: 14,
  }),
  progressStepLabel: style({
    fontSize: 12,
    color: '#0a0a0a',
    textAlign: 'center',
    maxWidth: 60,
  }),
};
